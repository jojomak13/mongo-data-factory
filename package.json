{
  "name": "mongo-data-factory",
  "version": "1.0.1",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "/build/**/*"
  ],
  "scripts": {
    "clean": "rm -rf ./build/*",
    "build": "npm run clean && tsc",
    "pub": "git add . && git commit -m \"make updates\" && npm run build && npm version patch && npm publish"
  },
  "keywords": [],
  "author": "Joseph Makram",
  "license": "ISC",
  "dependencies": {
    "faker": "^5.5.1",
    "mongoose": "^5.12.2"
  },
  "devDependencies": {
    "@types/faker": "^5.1.7",
    "typescript": "^4.1.3"
  }
}
